# HGF 门禁工具链依赖锁定（V3.3.2 修复：真 requirements 文件）
#
# V3.3.2（自审查 S3 修复）：此前本文件是 Markdown 文档（含说明/表格），
# `pip install -r requirements-hgf.txt` 直接报 Invalid requirement——违反
# HGF 自己的"文件存在 ≠ 它工作"纪律。现改为纯 pip 可解析格式：
#   每行一个包 + 固定版本（==），与 gate_plugins 的 min/max_version 契约对应。
# 说明文档已移至 README.md「工具链依赖」章节。
#
# 安装：python -m pip install -r requirements-hgf.txt
# 扫描：safety check -r requirements-hgf.txt --json

ruff==0.16.3
pytest==9.1.1
pytest-cov==7.1.0
detect-secrets==1.5.0
safety==3.8.1
semgrep==1.173.0
checkov==3.3.11
PyYAML==6.0.3
structlog==26.1.0
