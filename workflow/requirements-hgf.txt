# HGF 门禁工具链依赖（V3.2.5 固定版本）

用途：HGF 门禁工具自身的依赖锁定（#8 工具链自身安全——门禁检查依赖安全，
HGF 工具链也必须可复现、可扫描）。

```powershell
# 安装（对齐 workflow 门禁所需版本）
python -m pip install -r requirements-hgf.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 用 dependency_scan 门禁扫描本文件自身：
#   safety check -r requirements-hgf.txt --json
```

## 版本区间契约（与 gate_plugins 的 min/max_version 对应）

| 包 | 固定版本 | 解析契约版本区间 | 说明 |
|----|----------|------------------|------|
| ruff | ==0.16.3 | >=0.16 | 规则集按 0.16 固定（pyproject.toml [tool.ruff.lint]） |
| pytest | ==9.1.1 | >=7.0 | pythonpath 配置项需 >=7 |
| pytest-cov | ==7.1.0 | — | 覆盖率统计 |
| detect-secrets | ==1.5.0 | >=1.0 | JSON 解析契约 1.x |
| safety | ==3.8.1 | >=2.0,<4.0 | 3.x 顶层为字典（解析器已适配）；需 SAFETY_API_KEY |
| semgrep | ==1.173.0 | >=1.0 | JSON 解析契约 1.x |
| checkov | ==3.3.11 | >=1.0,<4.0 | 3.x 顶层为数组（解析器已适配） |
| PyYAML | ==6.0.3 | — | 配置加载 |
| structlog | ==26.1.0 | — | 结构化日志 |
