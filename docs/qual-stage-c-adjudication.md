# 阶段 C（审查环节优化）综合审议裁决书——v2.1 修正清单

日期：2026-08-19
依据：HeavySkill 4 轨迹审查（全部"需修改后通过"）+ 综合审议（源码实证交叉验证）
结论：**需修改后通过**——方向与算术成立（分层增量+预算与 v3.1 兼容），但 1 个 P0 回归风险 + 4 个 P0 缺项必须先修

---

## 一、综合审议的独立实证（读源码验证）

| 证据 | 结论 |
|---|---|
| C4-3↔缺陷1 冲突成立 | v3-arch:264/272 确认 PASS=`kept=∅ ∧ not any(e["exempted"])`，DEGRADE 要求 remaining=豁免项+报告注明豁免数；C4-3"不再每轮重复上报"若实现为"移出 remaining 不阻断"→ test_exemption_failclosed 必红 |
| 依赖图无来源成立 | incremental_checker.py:139 get_affected_chapters 需外部 chapter_dependencies，全仓仅测试夹具；单层非传递闭包 |
| 红队无回流成立 | gate8.py auto_fix_p0=False + max_retries=1，fatal→FAIL 无回流 |
| FixVerification 语义错配成立 | gate_regression.py verify_fix 仅 run_test/syntax，无"验证 issue 修复"能力 |
| 验收口径全错 | 跨章 4→1 漏 data_repair.py:304 第二套实现；占位符 Gate4 5 pattern vs Gate8 3 pattern（待填写/TBD 逃出）；锚点表实证 10 处调用点；70→35 仅 -50% 非 60-70% |
| 共识项验证通过 | 21 次/轮=深度11+结论1+辩论9；事实/假设实为静态（评审误标 LLM）；logic 缓存失效未定义；conclusion 只取前 5 章；ConsistencyIssue 双端点 |

---

## 二、最终修正清单

### P0（7 项，必须修正后才能实施）

| # | 缺陷 | 修正方案 | 涉及文件 | 验收标准 |
|---|---|---|---|---|
| P0-1 | **C4-3 豁免 fail-closed 接线**（回归风险） | exempt 状态与累积豁免清单**同源不可清除**，永不 PASS，仅"不重复触发修复投入"；DEGRADE 仍报告豁免数 | review_repair_loop.py C4-3 | test_exemption_failclosed 保持绿；豁免问题仍计入收敛判定 |
| P0-2 | **受影响集依赖图无来源+无闭包** | 定义 chapter_dependencies 构建源（跨章矛盾对 + 章节主题静态规则）+ 传递闭包；归属失败降级全量 | incremental_checker.py | 修 ch5 → ch7/ch10 均入受影响集；回归单测覆盖"修 A 破坏 B" |
| P0-3 | **红队 fatal 无回流** | 红队 fatal → 预算内回流 Gate4 限 1 轮（或阻断即最终结果，二选一明确） | gate8.py | 红队 fatal 有处理路径；不破坏 M1"跑得完" |
| P0-4 | **FixVerification 语义错配** | 扩展按 issue_id 重跑归属检查器（rerun_checker）方法 | gate_regression.py | 验证"issue 修复后消失"能力 |
| P0-5 | **验收口径 4 处修正** | 跨章改"审查路径去重至 1 处有效执行"（补 data_repair 统一项）；占位符"L1+G8 两处+统一 pattern 常量"；锚点"审查环节内 10 处→1 处"；降幅改"≤35 次+典型降 60%" | 路线图 M6 | 验收数字与工作包闭合 |
| P0-6 | **C4-1 子预算与 200 嵌套** | 定义 35 ⊂ 200 核算关系；超预算行为明确（建议 fail-closed 降级全量静态+显式"未审完"标注）；worst case 37>35 处置 | 预算机制 | 超预算不静默放行 |
| P0-7 | **C1-1 缓存失效语义** | 缓存键含章节内容版本，_repair_chapters 后失效/递增；execute/check_criteria 用修复后数据 | gate4.py | 轮末修复引入矛盾可检出 |

### P1（5 项，实施中一并处理）

| # | 缺陷 | 修正 |
|---|---|---|
| P1-1 | 报告级检查器作用域 | 定义"任一章节修改后报告级检查器（结论/红队）必须重审"，计入预算 |
| P1-2 | 跨章 issue 双端点归属 | ConsistencyIssue chapter1/chapter2 双端入受影响集，修复后两章均重审 |
| P1-3 | C4-2 去"正交"表述 | 明确与 A 缺陷6 同点叠加（同一循环函数）；"分"定义（签名集） |
| P1-4 | C1-4 锚点范围 10 处 | 锚点单例化覆盖全部 10 个调用点；DataAnchor 只读约束 |
| P1-5 | 占位符统一 pattern 常量 | 统一 L1/Gate3/Gate4a/Gate8 pattern 列表（含待填写/TBD），G8 用全量 |

### P2（3 项，可选）

| # | 缺陷 | 修正 |
|---|---|---|
| P2-1 | C0-1 改造范围低估 | 单列横切改造清单（全部检查器 dataclass ≥10 个 + 全部消费方）+ 回归测试 |
| P2-2 | Gate3 spec 同步 | 裁剪 Gate3 占位符检查时同步 pass_criteria |
| P2-3 | 成本基数口径统一 | 统一 LLM 计数口径（首轮 21 = 深度 11+结论 1+辩论 9） |

---

## 三、路线图 v2.0 → v2.1 更新内容

1. **C0-C4 每项标注修订**：C2-1 采纳原样；其余 13 项按上表修改后采纳
2. **验收口径修正**：M6 改"审查路径去重（跨章 1 处有效执行/占位符 2 处统一常量/锚点审查环节 1 处）+ LLM 审查 ≤35 次+典型降 60%"
3. **依赖矩阵新增**：C2-3←C0（受影响集依赖问题身份）、C4-2←A1（单调守卫同函数）、C4-3←A1（豁免同源）、C1-4←B2b（锚点数据源扩展后单例化）、C1-1←C0（缓存键需 issue 版本）
4. **版本记录**：v2.1 阶段 C 修订

---

## 四、最终判定

阶段 C 的**方向与收益核算成立**（分层增量+预算与 v3.1 兼容，LLM 降 60-70% 经源码核算），但存在：
- **1 个 P0 回归风险**：C4-3 豁免接线不当会推翻 A 阶段缺陷1 修复（豁免 PASS 绕过）
- **4 个 P0 缺项**：依赖图来源、红队回流、FixVerification 扩展、验收口径修正

按 P0 七项 + P1 五项修正后，阶段 C 可作为"审查高效不重复、报告质量不降"的可靠方案；路线图需更新为 v2.1。
